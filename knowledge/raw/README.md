# Raw Knowledge

这里是入口，不是终点。

- 文件名使用 `YYYY-MM-DD-topic.md`。
- 可以记录未完成观察、失败、原始线索和待验证假设。
- 必须脱敏；不要粘贴完整凭据或原生聊天正文。
- 超过 `workspace.toml` 的 `raw_ttl_days` 后，只能选择：整理进 `knowledge/curated/`、删除无价值内容、或在 `governance/debts.toml` 精确认领延期。
